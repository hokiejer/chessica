use crate::reset::Reset;

impl Reset {
    pub fn generate_next_white_bishop_move(&mut self, child: &mut Reset) {
    }

    pub fn generate_next_black_bishop_move(&mut self, child: &mut Reset) {
    }

}

